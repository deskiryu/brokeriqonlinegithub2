

@page "/customeredit/{CustomerId}"
@using ReviewIt.Web.Models
@using ReviewIt.Dto.Enum
@using ReviewIt.Web.Server.Extensions
@using BlazorDateRangePicker
@using System.IO
@using ReviewIt.Web.Services.Interface

@inherits CustomerEditBase


@if (!Saved)
{
    <section class="Customer-detail">

        <h4 class="page-title">Add or edit Customer</h4>

        <EditForm Model="@Customer" OnValidSubmit="@HandleValidSubmit" OnInvalidSubmit="@HandleInvalidSubmit">

            <DataAnnotationsValidator />
            <ValidationSummary />

            <div class="col-12 row">

                <div class="col-xs-12 col-sm-12 com-md-12 col-lg-12">
                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">First Name</label>

                        <div class="col-sm-8">
                            <InputText id="lastName" class="form-control col-sm-8" @bind-Value="Customer.FirstName" placeholder="Enter first name for this customer"></InputText>
                            <ValidationMessage class="offset-sm-3 col-sm-8" For="@(() => Customer.FirstName)" />
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">Last Name</label>

                        <div class="col-sm-8">
                            <InputText id="lastName" class="form-control col-sm-8" @bind-Value="Customer.LastName" placeholder="Enter last name for this customer"></InputText>
                            <ValidationMessage class="offset-sm-3 col-sm-8" For="@(() => Customer.LastName)" />
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">Business Name</label>

                        <div class="col-sm-8">
                            <InputText id="lastName" class="form-control col-sm-8" @bind-Value="Customer.BusinessName" placeholder="Enter business name for this customer"></InputText>
                            <ValidationMessage class="offset-sm-3 col-sm-8" For="@(() => Customer.BusinessName)" />
                        </div>
                    </div>


                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">Telephone Number</label>
                        <div class="col-sm-8">
                            <InputText id="lastName" class="form-control col-sm-8" @bind-Value="@Customer.TelephoneNumber" placeholder="Enter telephone for this Customer"></InputText>
                            <ValidationMessage class="offset-sm-3 col-sm-8" For="@(() => Customer.TelephoneNumber)" />
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">Date Of Birth</label>
                        <div class="col-sm-8">
                            <DateRangePicker @bind-StartDate="DateOfBirth" SingleDatePicker="true" placeholder=" Enter DOB for this Customer" class="form-control col-sm-8" Culture="@(System.Globalization.CultureInfo.GetCultureInfo("en-GB"))"></DateRangePicker>
                            <ValidationMessage class="offset-sm-3 col-sm-8" For="@(() => Customer.DateOfBirth)" />
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">Address</label>
                        <div class="col-sm-8">
                            <InputText id="lastName" class="form-control col-sm-8" @bind-Value="@Customer.Address" placeholder="Enter address for this Customer"></InputText>
                            <ValidationMessage class="offset-sm-3 col-sm-8" For="@(() => Customer.Address)" />
                        </div>
                    </div>


                    <button type="submit" class="btn btn-primary edit-btn">Update Customer</button>
                    <a class="btn btn-outline-primary edit-outline-btn" @onclick="@NavigateToOverview">Cancel</a>



                </div>

            </div>

        </EditForm>

    </section>

    @code {

        DateTimeOffset? DateOfBirth
        {
            get { return GetDTtoDTO(Customer.DateOfBirth); }
            set => Customer.DateOfBirth = SetDTtoDTO(value);
        }

        DateTimeOffset? GetDTtoDTO(DateTime datetimeIn)
        {
            if (Customer != null && datetimeIn != default(DateTime))
            {
                var localTime1 = DateTime.SpecifyKind(datetimeIn, DateTimeKind.Local);
                DateTimeOffset localTime2 = localTime1;
                return localTime2;
            }
            else
            {
                return DateTimeOffset.Now;
            }
        }

        DateTime SetDTtoDTO(DateTimeOffset? value)
        {
            return (value.HasValue ? value.Value : DateTime.MinValue).ToLocalTime().DateTime;
        }

    }


}
else
{
    <div class="alert @StatusClass">@Message</div>
    <a class="btn btn-outline-primary" @onclick="@NavigateToOverview">Back to Customer list</a>
}