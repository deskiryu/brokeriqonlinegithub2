@if (CanShowControl())
{
    <MudButton Variant="Variant.Filled" Color="Color.Primary" Style="@($"color:{Colors.Shades.Black};")" OnClick="GetInsuranceQuote" class="ml-2 mb-2">
        Get Quote
    </MudButton>
}