<MudDialog>
    <DialogContent>
        <div class="d-flex flex-column align-center">
            <MudLink OnClick="SaveSampleFile" Color="Color.Tertiary">Download sample file</MudLink>

            <MudFileUpload T="IBrowserFile" FilesChanged="SaveFile">
                <ButtonTemplate>
                    <MudButton HtmlTag="label"
                               Variant="Variant.Filled"
                               Color="Color.Primary"
                               Class="mb-1"
                               Style="@($"color:{Colors.Shades.Black}")"
                               StartIcon="@Icons.Material.Filled.CloudUpload"
                               for="@context.Id">
                        Select CSV file
                    </MudButton>
                </ButtonTemplate>
            </MudFileUpload>
            <MudText Class="@CurrentFileClass">Current File : @CurrentFileName</MudText>

            <div Class="@SimulationResultClass">
                <MudText Class="mb-1" Style="@($"color:{Colors.Green.Darken1}")">@SucessfulRecordsMessage</MudText>
                <MudText Class="mb-1" Style="@($"color:{Colors.Red.Darken1}")">@ErrorRecordsMessage</MudText>
                <MudLink OnClick="SaveRecordsInError" Color="Color.Tertiary" Class="@ErrorRecordsClass">Save records in error</MudLink>
            </div>
        </div>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel" Disabled="IsBusy">Close</MudButton>
        <MudButton OnClick="ImportFromFile" Variant="Variant.Filled" Color="Color.Primary" Style="@($"color:{Colors.Shades.Black}")" Disabled="DisableImportButton">@ImportButtonText</MudButton>
    </DialogActions>
</MudDialog>